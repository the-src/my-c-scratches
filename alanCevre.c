#include <stdio.h>

void alanCevre() {
    int kisa = 5, uzun = 7;
    printf("Perimeter of the rectangle = %d\n",2*(kisa + uzun));
    printf("Area of the rectangle = %d square inches",kisa*uzun);
}